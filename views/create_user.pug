extend ./layout/common.pug
block content
  if errors
    each error in errors
      .alert.alert-danger= error
  h1.text-center Create user
  .container
    form.m-auto(action='', method="POST")
      include _form_create_user_field.pug